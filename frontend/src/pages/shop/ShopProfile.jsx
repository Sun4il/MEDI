import Card from "../../components/ui/Card";

const ShopProfile = () => (
  <div className="page page-card">
    <section className="section">
      <span className="eyebrow">Shop profile</span>
      <h1 className="page__title" style={{ fontSize: "2.2rem" }}>
        Shop profile
      </h1>
      <p className="page__subtitle">Keep location, contact, and verification details in sync for search ranking.</p>
    </section>

    <Card>
      <p className="card__meta">Profile placeholder.</p>
    </Card>
  </div>
);

export default ShopProfile;