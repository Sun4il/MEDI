import Card from "../../components/ui/Card";

const VerifyShops = () => (
  <div className="page page-card">
    <section className="section">
      <span className="eyebrow">Admin workflow</span>
      <h1 className="page__title" style={{ fontSize: "2.2rem" }}>
        Verify shops
      </h1>
    </section>

    <Card>
      <p className="card__meta">Pending shops list placeholder.</p>
    </Card>
  </div>
);

export default VerifyShops;